export interface Children {
    children: React.ReactNode;
}

export interface Cards {
    id: number
    img:string
    gender: string
    category:string
}

export interface Typography {
    css:string;
    text:string;
}